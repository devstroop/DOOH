@layout PublicLayout
@page "/about"

@attribute [Authorize]

<PageTitle>About</PageTitle>
<RadzenStack>
    @if (_page != null && !isLoading)
    {
        @((MarkupString)_page.Content)
    }
</RadzenStack>
